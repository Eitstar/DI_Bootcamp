class Video {
    constructor(title, uploader, time) {
        this.title = title
        this.uploader = uploader
        this.time = time
        watch: (() => {
            return (`${this.uploader} parameter watched all ${this.time}parameter of ${this.title} parameter!`)
        })
    }
}
// Instantiate a new Video instance and call the watch() method.
class classVideo extends Video {
    constructor(title, uploader, time) {
        super();
        // return console.log((watch))
    }
}
class classicVideo extends Video {
    constructor(title, uploader, time) {
        super();
        getFullInfo()

        this.filemdby = filemdby
        this.editor = editor
            // return (`${this.fillmedby} "Eitan" ${this.editor}`)

    }
}
let a = new Video();
console.log((a))
let b = new Video();
console.log((b))
    // const users = {
    //         user1: {
    //             age: 44,
    //             name: 'picard',
    //         },
    //         user2: {
    //             age: 12,
    //             name: 'sisko',
    //         },
    //         user3: {
    //             age: 109,
    //             name: 'janeway',
    //         },
    //     }
    //     //     // filter
    //     //     // for all users older than 30
    //     //     // and store their data in an array
    //     //     //     // use Array.filter Array.map
    //     // const arrUsers = [{ id: 'user1', age: 44, name: 'picard' }, { id: 'user3', age: 109, name: 'janeway' }, ];


// const filter = ((users) => {
//     return console.log(get)
// })

// filter(users)
//     // // const userArr = ((users) => {
//     // //     return console.log(users)
//     // // });
//     // // userArr(users)